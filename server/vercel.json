{
  "version": 2,
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["node_modules/@clerk/**"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api" }
  ]
}
